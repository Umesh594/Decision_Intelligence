apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: decision-simulator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: decision_simulator
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: decision-simulator-secrets
                  key: DATABASE_USERNAME
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: decision-simulator-secrets
                  key: DATABASE_PASSWORD
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: decision-simulator
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
